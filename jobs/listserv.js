const queue = require("../lib/queue")
const Parser = require("rss-parser")
const taskName = "listserv"

exports.add = async (data, opts) => {
  // TODO:
}

queue.process(taskName, async (job, data) => {
  //
})
